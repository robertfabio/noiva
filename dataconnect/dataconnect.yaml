specVersion: "v1"
serviceId: "noiva"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "noiva-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
